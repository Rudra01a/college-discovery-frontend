'use client'; import { useRouter, useSearchParams, usePathname } from 'next/navigation';
import { ChevronLeft, ChevronRight } from 'lucide-react';
import { cn } from '@/lib/cn'; interface PaginationProps { currentPage: number; totalPages: number;
} export function Pagination({ currentPage, totalPages }: PaginationProps) { const searchParams = useSearchParams(); const router = useRouter(); const pathname = usePathname(); if (totalPages <= 1) return null; const goToPage = (page: number) => { const params = new URLSearchParams(searchParams.toString()); params.set('page', String(page)); router.replace(`${pathname}?${params.toString()}`); window.scrollTo({ top: 0, behavior: 'smooth' }); }; // Calculate page range to show 
const range: (number | '...')[] = []; if (totalPages <= 7) { for (let i = 1; i <= totalPages; i++) range.push(i); } else { range.push(1); if (currentPage > 3) range.push('...'); for (let i = Math.max(2, currentPage - 1); i <= Math.min(totalPages - 1, currentPage + 1); i++) { range.push(i); } if (currentPage < totalPages - 2) range.push('...'); range.push(totalPages); } const PageBtn = ({ page, isActive }: { page: number | '...'; isActive?: boolean }) => { if (page === '...') { return <span className="px-2 text-[#6b7280]">…</span>; } return ( <button onClick={() => goToPage(page)} aria-current={isActive ? 'page' : undefined} aria-label={`Go to page ${page}`} className={cn( 'h-9 w-9 rounded-lg text-sm font-medium transition-all duration-150', 'focus:outline-none focus:ring-2 focus:ring-brand-500', isActive ? 'bg-[#111111] text-white shadow-sm' : 'text-[#444444] hover:bg-gray-100 :bg-slate-800' )} > {page} </button> ); }; return ( <nav role="navigation" aria-label="Pagination" className="flex items-center justify-center gap-1 mt-10" > <button onClick={() => goToPage(currentPage - 1)} disabled={currentPage === 1} aria-label="Previous page" className={cn( 'h-9 w-9 rounded-lg flex items-center justify-center', 'text-[#444444] transition-colors', 'hover:bg-gray-100 :bg-slate-800', 'disabled:opacity-40 disabled:cursor-not-allowed disabled:hover:bg-transparent' )} > <ChevronLeft className="h-4 w-4" /> </button> {range.map((page, i) => ( <PageBtn key={i} page={page} isActive={page === currentPage} /> ))} <button onClick={() => goToPage(currentPage + 1)} disabled={currentPage === totalPages} aria-label="Next page" className={cn( 'h-9 w-9 rounded-lg flex items-center justify-center', 'text-[#444444] transition-colors', 'hover:bg-gray-100 :bg-slate-800', 'disabled:opacity-40 disabled:cursor-not-allowed disabled:hover:bg-transparent' )} > <ChevronRight className="h-4 w-4" /> </button> </nav> );
}
