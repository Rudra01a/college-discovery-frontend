'use client'; import { useState } from 'react';
import { cn } from '@/lib/cn'; interface Tab { id: string; label: string; icon?: React.ReactNode; count?: number;
} interface TabsProps { tabs: Tab[]; activeTab: string; onChange: (id: string) => void; className?: string; variant?: 'underline' | 'pills';
} export function Tabs({ tabs, activeTab, onChange, className, variant = 'underline' }: TabsProps) { if (variant === 'pills') { return ( <div className={cn('flex gap-1 p-1 bg-gray-100 rounded-xl', className)} role="tablist" > {tabs.map((tab) => ( <button key={tab.id} role="tab" aria-selected={activeTab === tab.id} onClick={() => onChange(tab.id)} className={cn( 'flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-sm font-medium transition-all duration-150', activeTab === tab.id ? 'bg-white text-[#111111] shadow-sm' : 'text-[#444444] hover:text-[#111111] :text-slate-200' )} > {tab.icon} {tab.label} {tab.count !== undefined && ( <span className="ml-1 text-2xs bg-brand-100 text-[#111111] px-1.5 py-0.5 rounded-full"> {tab.count} </span> )} </button> ))} </div> ); } // Underline variant 
return ( <div className={cn('flex gap-0 border-b border-gray-200 ', className)} role="tablist" > {tabs.map((tab) => ( <button key={tab.id} role="tab" aria-selected={activeTab === tab.id} onClick={() => onChange(tab.id)} className={cn( 'flex items-center gap-1.5 px-4 py-3 text-sm font-medium', 'border-b-2 transition-all duration-150 -mb-px', 'focus:outline-none focus:ring-2 focus:ring-brand-500 focus:ring-inset', activeTab === tab.id ? 'border-brand-600 text-[#111111] ' : 'border-transparent text-[#444444] hover:text-[#111111] hover:border-gray-200 :text-slate-200' )} > {tab.icon} {tab.label} {tab.count !== undefined && ( <span className="text-2xs text-[#6b7280] ">({tab.count})</span> )} </button> ))} </div> );
}
