import { cn } from '@/lib/cn'; interface EmptyStateProps { icon?: React.ReactNode; title: string; description?: string; action?: React.ReactNode; className?: string;
} export function EmptyState({ icon, title, description, action, className }: EmptyStateProps) { return ( <div className={cn( 'flex flex-col items-center justify-center text-center py-16 px-6', className )} > {icon && ( <div className="w-16 h-16 rounded-2xl bg-gray-100 flex items-center justify-center mb-4 text-[#6b7280] "> {icon} </div> )} <h3 className="text-lg font-semibold text-[#111111] mb-1">{title}</h3> {description && ( <p className="text-sm text-[#6b7280] max-w-sm mb-6">{description}</p> )} {action && !description && <div className="mt-6">{action}</div>} {action && description && <div>{action}</div>} </div> );
}
