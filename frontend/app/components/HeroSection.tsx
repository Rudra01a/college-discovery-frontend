'use client'; import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { Search, ArrowRight, Sparkles } from 'lucide-react';
import { Button } from '@/components/ui/Button';
import { cn } from '@/lib/cn'; const QUICK_SEARCHES = [ 'IIT Bombay', 'MBBS Delhi', 'MBA Bangalore', 'NIT Trichy', 'Law Colleges', 'Design Colleges',
]; export function HeroSection() { const [query, setQuery] = useState(''); const router = useRouter(); const handleSearch = (q?: string) => { const searchQuery = q ?? query; if (searchQuery.trim()) { router.push(`/colleges?search=${encodeURIComponent(searchQuery.trim())}`); } else { router.push('/colleges'); } }; return ( <section className="relative bg-gradient-to-b from-brand-50/50 via-white to-white overflow-hidden"> {/* Background decoration */} <div className="absolute inset-0 pointer-events-none overflow-hidden"> <div className="absolute -top-20 -right-20 w-[500px] h-[500px] bg-brand-100/60 rounded-full blur-3xl" /> <div className="absolute top-40 -left-20 w-[400px] h-[400px] bg-sky-100/50 rounded-full blur-3xl" /> </div> <div className="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-16 pb-20 md:pt-24 md:pb-28"> {/* Pill badge */} <div className="flex justify-center mb-6"> <span className="inline-flex items-center gap-1.5 px-3.5 py-1.5 bg-gray-50 text-[#111111] border border-brand-200 rounded-full text-xs font-bold shadow-sm tracking-wide uppercase"> <Sparkles className="h-3.5 w-3.5" /> India&apos;s most trusted college platform </span> </div> {/* Headline */} <h1 className="text-4xl md:text-5xl lg:text-6xl font-extrabold text-[#111111] text-center leading-tight text-balance mb-5 tracking-tight"> Find your perfect{' '} <span className="text-[#111111] ">college</span>{' '} in India </h1> <p className="text-lg md:text-xl text-[#444444] text-center max-w-2xl mx-auto mb-10 text-balance leading-relaxed"> Search, compare, and shortlist from 10,000+ colleges across engineering, medical, MBA, law, and more. All data in one place. </p> {/* Search bar */} <div className="max-w-2xl mx-auto mb-8"> <div className="flex gap-2 p-1.5 bg-white rounded-2xl shadow-card-lg border border-gray-200 focus-within:border-brand-400 :border-brand-600 focus-within:ring-2 focus-within:ring-brand-100 :ring-brand-900/20 transition-all"> <div className="flex items-center gap-3 flex-1 pl-4"> <Search className="h-5 w-5 text-[#444444] shrink-0" /> <input id="hero-search" type="text" value={query} onChange={(e) => setQuery(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && handleSearch()} placeholder="Search by college name, location, or course..." className="flex-1 bg-transparent text-[#111111] placeholder:text-[#6b7280] text-base focus:outline-none py-2.5 font-medium" /> </div> <Button variant="primary" size="lg" rightIcon={<ArrowRight className="h-5 w-5" />} onClick={() => handleSearch()} id="hero-search-btn" className="px-6 md:px-8 shadow-md" > <span className="hidden sm:inline">Search Colleges</span> <span className="sm:hidden">Search</span> </Button> </div> </div> {/* Quick searches */} <div className="flex flex-wrap justify-center items-center gap-2.5"> <span className="text-xs font-bold uppercase tracking-wider text-[#6b7280] ">Popular:</span> {QUICK_SEARCHES.map((q) => ( <button key={q} onClick={() => handleSearch(q)} className="text-xs font-medium px-3.5 py-1.5 bg-white border border-gray-200 text-[#444444] rounded-full hover:border-brand-400 hover:text-[#111111] hover:bg-gray-50 :border-brand-600 :text-brand-400 :bg-slate-700 transition-all shadow-sm" > {q} </button> ))} </div> </div> </section> );
}
