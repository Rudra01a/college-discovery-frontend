'use client'; import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { Toaster } from 'react-hot-toast';
import { useState } from 'react'; export function Providers({ children }: { children: React.ReactNode }) { const [queryClient] = useState( () => new QueryClient({ defaultOptions: { queries: { staleTime: 1000 * 60 * 5, retry: 2, refetchOnWindowFocus: false, }, }, }) ); return ( <QueryClientProvider client={queryClient}> <> {children} <Toaster position="bottom-right" toastOptions={{ duration: 3000, style: { borderRadius: '12px', fontSize: '14px', fontWeight: '500', color: '#111111', border: '1px solid #e5e7eb', boxShadow: '0 4px 12px rgba(0,0,0,0.05)' }, success: { style: { background: '#ffffff', color: '#111111', border: '1px solid #e5e7eb' }, iconTheme: { primary: '#111111', secondary: '#ffffff' }, }, error: { style: { background: '#ffffff', color: '#dc2626', border: '1px solid #fecaca' }, iconTheme: { primary: '#dc2626', secondary: '#ffffff' }, }, }} /> </> </QueryClientProvider> );
}
